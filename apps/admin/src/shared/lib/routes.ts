export const ROUTES = {
  LOGIN: '/login',

  // 운영 화면 4종 — H1 에선 placeholder, 다음 핸드오프에서 실제 화면으로 교체
  EVENTS: '/events',
  ANNOUNCEMENTS: '/announcements',
  INQUIRIES: '/inquiries',
  OPS: '/ops',
} as const
